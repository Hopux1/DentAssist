@model IEnumerable<DentAssist.Models.PlanTratamiento>

@{
    ViewData["Title"] = "Planes de Tratamiento";
}

<h1 class="mb-4">Lista de Planes de Tratamiento</h1>

<p>
    <a asp-controller="PlanesTratamiento" asp-action="Create" class="btn btn-primary">Agregar Plan</a>
</p>

<form method="get" class="mb-3">
    <div class="input-group">
        <input type="text" name="buscarPaciente" value="@ViewBag.FiltroPaciente" class="form-control" placeholder="Buscar por nombre del paciente..." />
        <div class="input-group-append">
            <button class="btn btn-outline-secondary" type="submit">Buscar</button>
        </div>
    </div>
</form>

<table class="table table-striped">
    <thead class="table-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Observaciones)</th>
            <th>Paciente</th>
            <th>Odontólogo</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Observaciones</td>
                <td>@item.Paciente?.Nombre</td>
                <td>@item.Odontologo?.Nombre</td>
                <td>
                    <a asp-controller="PlanesTratamiento" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Editar</a>
                    <a asp-controller="PlanesTratamiento" asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Detalles</a>
                    <a asp-controller="PlanesTratamiento" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
